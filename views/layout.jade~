!!! 5
html
  head
    meta(charset="utf-8")
    title Bass Room Stats
    script(src="https://raw.github.com/timrwood/moment/2.0.0/min/moment.min.js")
    meta(itemprop="name", content="Bass Room Stats")
    meta(itemprop="description", content="Bass Music Room on Plug.dj")
    meta(itemprop="image", content="/img/mixer.jpg")

    meta(name="viewport", content="width=device-width, initial-scale=1.0")

    style
      .subhead {
        margin-top: 40px;
      }
    link(rel="stylesheet", href="/css/bootstrap.css")
    style
      #wide {
        background-repeat: no-repeat;
        background-position: center;
      }
      .stage {
        text-align: center;
        margin-left: auto;
        margin-right: auto;
        width: 60%;
      }
      .stage a {
        font-size: 1.5em;
        font-weight: bold;
        width: 20%;
        display: inline-block;
      }
      .stage a:hover, .chat-message a:hover {
        text-decoration: none;
      }
      .stage a span {
        display: block;
      }
      .plug-sprite {
        width: 150px;
        height: 150px;
      }
      footer {
        font-size: 0.8em;
      }

    link(rel="stylesheet", href="/css/bootstrap-responsive.css")
    link(rel="stylesheet", href="/css/screen.css")

  body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          ul.nav
            li.active
              a(href="/") Home
            li
              a(href="/songs") Songs
            li
              a(href="/djs") DJs
            li
              a(href="/history") History
            li
              a(href="/chat") Chat

    if wideformat
      #wide
        block content
    else 
      block content

    .container
      footer
        hr
        .row
          .span12

            h5
              a(href="/commands") Command List
              |  &bull; 
              a(href="https://plus.google.com/103972776808088576698", rel="publisher") Facebook


            p Started listening on #{oldestPlay.timestamp}.



    script(src="//code.jquery.com/jquery-1.8.3.min.js")
    script(src="/js/bootstrap.js")
    script(src="/js/jquery.timeago.js")
    

    script
      $('.plug-sprite').each(function() {
        var self = this;
        setInterval(function() {
          $(self).css('background-position-x', (parseInt($(self).css('background-position-x')) - 150) + 'px');
        }, 83);
      });

      $('.timeago').timeago();

    block scripts

    - if (typeof(config.ga.account) != 'undefined' && config.ga.account.length > 0)
      script(type="text/javascript")
        var _gaq = _gaq || [];
        _gaq.push(['_setAccount', '#{config.ga.account}']);
        _gaq.push(['_trackPageview']);

        (function() {
          var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
          ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
          var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
        })();

        (function() {
          var po = document.createElement('script'); po.type = 'text/javascript'; po.async = true;
          po.src = 'https://apis.google.com/js/plusone.js';
          var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(po, s);
        })();
